#include<iostream>
using nameapace std;
struct list
{
    int data
    struct list *next;
}
int main()
{
    int n,k,a;
    struct *head,*p,*q,*t;
    cin >> head >> n >> k;
    for(int i=0;i<n;i++){
    cin >> p >> a >> q;
    p->data=a;
    }
    int sum=0;

}